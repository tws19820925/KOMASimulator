name: Static Site Validation

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Validate static site
        shell: pwsh
        run: ./tools/Validate-StaticSite.ps1
